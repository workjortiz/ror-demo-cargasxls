<p style="color: green"><%= notice %></p>

<%= render @currency %>

<div>
  <%= link_to "Edit this currency", edit_currency_path(@currency) %> |
  <%= link_to "Back to currencies", currencies_path %>

  <%= button_to "Destroy this currency", @currency, method: :delete %>
</div>
